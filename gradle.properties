org.gradle.jvmargs=-Xmx2G
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true

minecraft_version=26.3
loader_version=0.19.5
fabric_api_version=0.160.5+26.3

loom_version=1.17.17

mixinextras_api_version=0.5.5

mod_id=satchels
mod_name=Hotbar Satchels
mod_version=1.2
mod_license=MIT

mod_group_id=net.hotbar
mod_authors=Winord
mod_description=Equippable satchels providing extra hotbar storage.
trinkets_version=4.2.0+26.3
ohmega_version=1.5.21
forgeconfigapiport_version=26.3.0
jei_version=31.0.0.11
# TODO(26.3): Raised still has no build under 26.3 (confirmed absent, see
# satchels-port-decisions-26_3.md §0/§4.7) - decision is to NOT cut it, wait for a real build,
# and try the same version-bump-only path that worked for 26.1->26.2. Value below is still the
# last known-good 26.2 build; RaisedCompat only activates if the mod is actually present at
# runtime, so shipping without a 26.3 Raised build does not block the rest of the port.
raised_version=Fabric-26.2-5.1.2
cloth_config_version=26.3.158
modmenu_version=21.0.0-beta.1