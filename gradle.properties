org.gradle.jvmargs=-Xmx2G
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true

minecraft_version=26.2
loader_version=0.19.4
fabric_api_version=0.160.0+26.2

loom_version=1.17.17

mixinextras_api_version=0.5.5

mod_id=satchels
mod_name=Hotbar Satchels
mod_version=1.2
mod_license=MIT

mod_group_id=net.hotbar
mod_authors=Winord
mod_description=Equippable satchels providing extra hotbar storage.

# Still archived on 26.2 (SatchelsCompat.TRINKETS.shouldLoad stays false) - see
# satchels-port-decisions-26_2.md §2. The desync-fix claim for 4.1.0-rc.1+26.2+ is NOT
# confirmed by upstream changelog (checked 2026-09-18); only 4.1.0-beta.1+26.2 exists so far,
# and its changelog only covers equipped()/allEquipped() API + Tuple deprecation, no mention
# of the slot-id/visual-position desync fix. Version bumped only so the compileOnly/localRuntime
# dependency resolves against 26.2; do not flip shouldLoad without re-verifying this.
trinkets_version=4.1.0-beta.1+26.2
# Only Fabric build for 26.2 so far is the 1.6.0-beta.x line (1.5.x never got a 26.2 Fabric
# build - 1.5.21's Fabric build stops at 26.1.2, its 26.3 build is the "next" stable jump).
# Confirm this still loads cleanly since it's a beta.
ohmega_version=1.6.0-beta.19
forgeconfigapiport_version=26.2.1

# 26.2 JEI is still beta-only (no "R" release found as of 2026-09-18) - 30.x branch confirmed.
jei_version=30.25.0.177

raised_version=Fabric-26.2-5.1.2
cloth_config_version=26.2.155
modmenu_version=20.0.1
